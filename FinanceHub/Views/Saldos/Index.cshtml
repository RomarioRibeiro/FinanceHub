@model IEnumerable<FinanceHub.Models.Saldo>

@{
    ViewData["Title"] = "Lista de Saldos";
}

<h2>@ViewData["Title"]</h2>
<table class="table">
    <thead>
        <tr>
            <th>ContaId</th>
            <th>Data</th>
            <th>Valor</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.ContaId</td>
            <td>@item.Data</td>
            <td>@item.Valor</td>
            <td>
                <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.Id">Detalhes</a>
            </td>
        </tr>
    }
    </tbody>
</table>
